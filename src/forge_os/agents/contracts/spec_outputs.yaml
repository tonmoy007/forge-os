schema_version: '0.1'
id: spec_outputs
stage_id: spec
persona_id: spec_writer
required_outputs:
- id: spec_primary_artifact
  type: file_exists
  path: pipeline/log/spec-agent-output.md
  description: Primary deterministic output for the spec stage.
  blocking: true
